{% extends 'base.html.twig' %}

{% block title %}Maincustomer{% endblock %}

{% block body %}
    <h1>Maincustomer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maincustomer.id }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ maincustomer.contact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_maincustomer_index') }}">back to list</a>

    <a href="{{ path('app_maincustomer_edit', {'id': maincustomer.id}) }}">edit</a>

    {{ include('maincustomer/_delete_form.html.twig') }}
{% endblock %}
