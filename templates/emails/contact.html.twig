<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
	</head>
	<body>
		{# Template Email for the first contact #}
		<h1>Demande de
			{{ contact.fullName }}</h1>

		<p>Email :
			{{ contact.email }}</p>
		<p>Société :
			{{ contact.subject }}</p>
		<p>Message :</p>
		<div>
			{{ contact.message|raw }}
		</div>
	</body>
</html>
