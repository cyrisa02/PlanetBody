{% extends 'base.html.twig' %}

{% block title %}Partner
{% endblock %}

{% block body %}
	<h1>Partner</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ partner.id }}</td>
			</tr>

			<tr>
				<th>Contract</th>
				<td>{{ partner.contract }}</td>
			</tr>
			<tr>
				<th>Is_enable</th>
				<td>{{ partner.isEnable ? 'Yes' : 'No' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_partner_index') }}">back to list</a>

	<a href="{{ path('app_partner_edit', {'id': partner.id}) }}">edit</a>

	{{ include('pages/partner/_delete_form.html.twig') }}
{% endblock %}
