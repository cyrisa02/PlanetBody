{% extends 'base.html.twig' %}

{% block title %}Activation du  Statut
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Désactiver / Activer un compte Franchisé</h1>
		<h2>Nom du franchisé:{{ partner.user.name }}

		</h2>
		<h3>N°:{{ partner.id }}</h3>


		{{ include('pages/partner/_form.html.twig', {'button_label': 'Enregistrer'}) }}

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_partner_index') }}">Retour à la liste</a>


		{{ include('pages/partner/_delete_form.html.twig') }}</div>

{% endblock %}
