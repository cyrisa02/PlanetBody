{% extends 'base.html.twig' %}

{% block title %}Activation du Statut
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Liste des permissions pour le franchisé
			{{ partner.user.name }}
			N°

			{{ partner.id }}
		</h1>


		{{ include('pages/partner/_form.html.twig', {'button_label': 'Enregistrer'}) }}

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_partnerpermission_index') }}">Retour à la liste</a>


		<h1>ENvoyer un email</h1>
		{{ include('pages/partner/_delete_form.html.twig') }}</div>

{% endblock %}
