<form method="post" action="{{ path('app_mailing_delete', {'id': mailing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mailing.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Supprimer</button>

</form>
