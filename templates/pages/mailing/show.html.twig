{% extends 'base.html.twig' %}

{% block title %}Mail
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Mail</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ mailing.id }}</td>
				</tr>
				<tr>
					<th>Titre</th>
					<td>{{ mailing.title }}</td>
				</tr>
				<tr>
					<th>Message</th>
					<td>{{ mailing.content }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_mailing_index') }}">Retour à la liste</a>


		<a class="btn btn-primary btn-lg mt-4" href="{{ path('app_mailing_edit', {'id': mailing.id}) }}">Modifier</a>


		{{ include('pages/mailing/_delete_form.html.twig') }}</div>

{% endblock %}
