{% extends 'base.html.twig' %}

{% block title %}Sentmail
{% endblock %}

{% block body %}
	<h1>Sentmail</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ sentmail.id }}</td>
			</tr>
			<tr>
				<th>CreatedAt</th>
				<td>{{ sentmail.createdAt ? sentmail.createdAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_sentmail_index') }}">back to list</a>

	<a href="{{ path('app_sentmail_edit', {'id': sentmail.id}) }}">edit</a>

	{{ include('pages/sentmail/_delete_form.html.twig') }}
{% endblock %}
