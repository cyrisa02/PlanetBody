<form method="post" action="{{ path('app_sporthall_delete', {'id': sporthall.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sporthall.id) }}">
    <button class="btn">Delete</button>
</form>
